[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["mccole"]

[tool.setuptools.package-data]
"*" = ["data/**/*"]

[project]
name = "mccole"
version = "0.6.3"
requires-python = ">=3.12"
authors = [
    {name = "Greg Wilson", email = "gvwilson@third-bit.com"},
]
maintainers = [
    {name = "Greg Wilson", email = "gvwilson@third-bit.com"},
]
description = "Simple static site generator for tutorials"
keywords = ["open source", "static site generator"]
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "beautifulsoup4",
    "html5validator",
    "jinja2",
    "markdown",
    "tomli"
]

[project.optional-dependencies]
dev = [
    "build",
    "coverage",
    "markdown-include",
    "mkdocs",
    "mkdocstrings[python]",
    "pytest",
    "ruff",
    "twine"
]

[project.scripts]
mccole = "mccole:clui.main"
